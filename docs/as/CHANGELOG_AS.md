# CHANGELOG_AS

Contract changes made during the build. Only the human adds entries (after resolving a
`SPEC_ISSUES.md` item). Each entry: date, SI number, what changed (files, fields, rules), why,
and which protected tests were updated. A change here is the only legitimate way a protected
file changes after `as-kit-baseline`. After editing the protected files, the human (never the agent)
refreshes the hashes: `AS_MAINTAINER=1 python tools/as/protect.py --write-manifest`.

| Date | SI | Change | Files | Tests updated |
|---|---|---|---|---|
| — | — | Build spec v1 baseline (tag `as-kit-baseline`) | all | all |
| 2026-09-22 | kit update P7 | P7 contracts and tests added (the slice). Earlier phases amended: **P0** `kernel/clock.begin_turn` (a P7 stub in a P0 module), `Store.rebuild_fts` and `kernel/store.wall_clock_iso` (implemented; `Store.create`'s created_at_real now comes from it, because the gate's scan forbids importing datetime outside lanes/), bit G08 checks `ownership.EVENT_WRITERS` (the resolver and propagation write events but own no table), events `NARRATION`, `ECHO_RECORD`, `PENDING_REACTION`; **P1** `lanes/calllog.request_hash` (implemented) and `ReplayTransport` finds a call by request hash and replays recorded failures (D-21); **P5** `action/reactions.next_wave(..., exclude=)`; **testing** `ScenarioWorld.session(run_dir=None)` (a P7 stub in the scenario loader: the slice tests play a loaded scenario as a session) | kernel/clock.py, kernel/store.py, kernel/ownership.py, audit/commit_gate.py, contracts/events.py, lanes/calllog.py, action/reactions.py, testing/scenario.py; new turn/select.py, intake.py, cognition.py, timers.py, lanes/requests.py, service/replay.py | p01_lanes/test_calllog.py (+1: `test_replay_matches_by_request_not_by_position`); new p07_slice/*, sim/test_soak_metal_fence.py, fixtures/scenarios/fence_climb.yaml |
| 2026-09-23 | kit update P8 | P8 contracts and tests added (the Play UI): `service/game_service.py` (the whole protocol, PROTO-01..10; turns as background tasks, Stop, reconnect, busy, not_built_yet for later phases), `service/guide.py` (Ask), `contracts/protocol.py` (IN_MODELS / OUT_MODELS; OutConfig, PackView, OutPacks, OutRunLoaded, OutSaved, OutSettings, OutDevData; `settings_get` / `settings_set`; InContentValidate; more OutTurnRejected codes), the Play UI vitest specs and fixtures. Earlier phases amended: **P0** `Store.transaction` rolls back on any BaseException (a cancelled turn); **P7** `service/session` gains CHANGEABLE_SETTINGS, LockedSetting, `change_settings` (P8 stub), `service/replay.resimulate` re-applies mid-run settings changes (SET-01), the pipeline docstring says how a cancelled turn behaves; **content** none | kernel/store.py, contracts/protocol.py, service/game_service.py, service/session.py, service/replay.py, turn/pipeline.py; new service/guide.py; docs 02, 04, 10, 11, 12, 13, DECISIONS D-24..D-30 | p00_substrate/test_store.py (+1: `test_a_cancelled_task_rolls_back_too`); new p08_ui_protocol/*, talemate_frontend/src/play/__tests__/* |
| 2026-09-23 | kit update P9 | P9 contracts and tests added (society): `society/population.py`, `household.py`, `routine.py`, `work.py`, `settlement.py`, `group.py`, `world/rumours.py`, group standing in `mind/mind.py` (STAND-01..02). Earlier phases amended: **P0** `contracts/events.py` gains LOYALTY_CHECK, STANDING_CHANGE, ROLE_RELEASED, WORKPLACE_CHANGE, STORES_CHANGE, SETTLEMENT_DAY, SETTLEMENT_CHANGE, SHORTAGE_ENDED, ROUTINE_STEP, GROUP_DAY; `kernel/clock.QUEUE_TYPES` gains SETTLEMENT_DAY and GROUP_DAY, new `BACKGROUND_QUEUE_TYPES`; `contracts/settings.SocietyRules` (`RulesConfig.society`); `kernel/schema.sql` work_assignments key gains `shift_start_hh` (SCHEMA_VERSION unchanged — D-38); **P2** `physical/bodies.progress` clamps a need stage at 0; **P3** `mind/perception.grant(confidence=)`; **P5** `action/cascade.py` P9 selectors, precondition paths and dispatch lines, CAS-06 scheduled effects and `fire_scheduled`; `action/intent.plan_continuation` step 5 (routines); **P7** `turn/timers.py` (P9 dispatch lines, `fire_one`, `fire_due`, `seed_society`, `run_offscreen`), `turn/select.horizon` (HOR-01: background rows are not news — D-32), `turn/pipeline` stages 0 and 12, `service/view` journal rumours; **content** core `cascade/economy.yaml`: CAS-001 targets the injured body's own posts (it named the attacker's), CAS-002 carries the missed shift's hours, new CAS-016 / CAS-017 (food shortage → ration); `cascade/people.yaml`: CAS-007 filters nothing and reads the dead body from `payload.body_id` (DEATH has no `kind` or `actor_id` field, so it never fired), SHIFT_MISSED payload `{reason: dead}`, new CAS-018 (a believed theft rumour costs trust); **fixtures** `pump_settlement.yaml` gains six households (Ramos, Walsh, Salazar, Tate, Lindqvist, Farrow) so every named person has one | contracts/events.py, contracts/settings.py, kernel/clock.py, kernel/schema.sql, physical/bodies.py, mind/perception.py, mind/mind.py, action/cascade.py, action/intent.py, turn/timers.py, turn/select.py, turn/pipeline.py, service/view.py, society/*, world/rumours.py, as_content/packs/core/cascade/economy.yaml, people.yaml; docs 03, 04, 06, 11, 12, 13, GLOSSARY, DECISIONS D-31..D-38; `tools/as/gate.py --write-rules` takes a content record's own `description` as its rule statement; new skill `.dsh/skills/as-society`, listed in AGENTS.md §6 | new p09_society/* (91 tests); fixtures/scenarios/pump_settlement.yaml; no earlier test changed |
| 2026-09-24 | kit update P10 | P10 contracts and tests added (the wide world): worldgen WG0–WG9 (`world/worldgen/*`: params, placement, region with the exterior and at most one wilds zone, causal history, polity, people, opening, checks, pipeline + genesis), `service/runs.create_run`, `service/background.py` (the quiet hours: reflection and retelling by simulated eligibility, finished before each move — BG-01..07), `service/progress.py` (the loading bar, PROG-01..07), `world/traces.py` (TRACE-01..06), `world/decay.py` (wear, WEAR-01..04 — replaces Memory Fade), `world/infected.py` (INF-01..13: senses, energy by time, doors under a crowd's strain, the dead rising as new bodies), `world/hordes.py` (HRD-01..17: counted pools, hordes, the exterior, the Mega Horde, the census), `world/factions.py` (FAC-01..06: enclave, council, route watch, DECON), `world/worldmove.py` (WORLD-02..06, OPS-01..08: the world's day, no death lottery, outings, raids), rumour retelling (INFO-06); GameService P10 handlers (`pcs_list`, `run_new`, `worldgen_cancel`, the quiet hours and the bars), CLI `new-run`, replay BG-05; the Play UI's New Life wizard, Worldgen screen and loading bar. Earlier phases amended: **P0** `contracts/events.py` (P10 world, horde, faction, trace, wear and background types), `contracts/settings.py` (WorldRules P10 fields — the death lottery and trace ratio retired, D-52 — InfectedRules, HordeRules, BackgroundRules, DecayRules), `contracts/content.py` (FactionBehaviour and its blocks, Leader.seat / age, InfectionStage felt / signs, QuipList), `contracts/protocol.py` + `worldgen.py` (progress messages; worldgen sub-steps), `kernel/clock.py` (INFECTED_STEP, WORLD_DAY, OPERATION_STEP, REANIMATION, TRACE_DECAY, HORDE_STEP, POOL_RISE, COUNCIL; background types), `kernel/ids.py` (hrd), `kernel/ownership.py` (world.factions writer-only), `kernel/schema.sql` (pools, hordes, infected_state columns, settlements.lockdown, operations.target_id, portals.strain_min, traces.locked — SCHEMA_VERSION unchanged, D-50); **P2** `physical/bodies.py` (create, expose, die, rise, stages; DEATH rise_pending and the REANIMATION row), `physical/space.py` (discover_layout, place_body, change_place, `path(allow_closed, allow_locked)`, portal strain_min), `physical/objects.py` (condition, made_at, contaminate / contaminated); **P3** `mind/perception.py` (trace percepts, "what was left of"); **P4** `mind/actor.create`, `mind/packet.py` (a host's felt lines); **P5** `action/effects.py` (a bite exposes and feeds, a drink is mouth contact, on_arrival after a MOVE), `action/propagate.py` (infected hear and are drawn; a loud enough sound draws a district), `action/cascade.py` (INFECTED_DRIFT, create_trace), `mind/cues.py` (a bite seen, a host's stage signs); **P7** `turn/cognition.py` (the week-3 compulsion as an INVOLUNTARY act, never the PC), `narration/narrator.py` (the PC's felt lines), `narration/location.py` (traces), `turn/timers.py` (P10 dispatch, seed_world), `turn/pipeline.py` (S0 seeds the world); **P8** `service/game_service.py` (P10 handlers and bars); **P9** `society/population.py` (take_from_cohort, materialise), `society/routine.py` (on_arrival; 'away' while on an outing), `society/settlement.py` (STL-13 lockdown; privation deaths rise), `world/rumours.py` (retell, spread_one, place rumours such as 'horde_coming'); **content** core `pathways.yaml` (wet: the day-3 saliva stage, felt and signs, `mouth_contact_item`), `cues.yaml` (spreader_signs), `infected/states.yaml` (bite_commitment now used), `factions/ghosts.yaml` + `lore/ghosts.md` (new, Ghosts_6; D-59), `mafia_remnants.yaml` (a relation to the Ghosts), `ui/quips.yaml` (new); CNT-15, CNT-16 | as above; docs 03, 05, 06, 09, 10, 13, DECISIONS D-39..D-62, GLOSSARY, SPEC_ISSUES, play/README.md | new p10_world/* (220 tests), new UI specs quips / loading_bar / wizard / worldgen (+ fixtures); amended p02_space_bodies/test_bodies.py (the DEATH payload carries `rise_pending`), p08_ui_protocol/protocol_kit.py (`pushed_after` leaves the bar's pushes out; `bar_after`), talemate_frontend app.spec.js (the wizard and worldgen screens are built) |
| 2026-09-24 | P10 play-through (DECISIONS D-63..D-66) | Contracts amended after playing generated worlds: **P3** `mind/perception.py` (a MOVE without an anchor reads 'arrives' or 'moves into <place>' for someone coming, 'moves away' only for someone leaving; the implemented `place_phrase` keeps a name's own article and a proper name's numbers and joining words); **P4** `mind/packet.py` (SKULL-10: nothing later than the moment reaches a mind), `mind/affordance.py` (SKULL-10; `requires.portal_kinds`; the speech and wound bindings skip the dead), `mind/retrieval.py` and `mind/memory.py` (the same selection, up to the moment); **P5** `action/reactions.py` (REACT-01: one of the dead moving to within 20 m is news); **P7** `turn/select.py` (SEL-02 / SEL-03 read percepts up to the wave's moment), `service/view.py` (the dice receipt names a defence, 'Resisting grab'; the suggestions never repeat a label); **P10** `world/worldgen/region.py` (each site's frontage on its street), `world/infected.py` (INF-02: `STILL_VERBS` — starting to watch, wait, keep guard, hide or talk is not movement); **P0 data** `contracts/content.py` (`AffordanceRequires.portal_kinds`); **content** core `affordances/portals.yaml` (the kinds of way each door-handling option fits), `affordances/social_body_attention.yaml` (calming, signalling and shielding are for people) | A turn failed on a percept from after its reaction's moment; the opening's dead reached the player in ten seconds down a street of no length; a watch lasted eight hours with the dead beside the player; options and words that made no sense on screen; docs 04, 05, 06, 09, 10, 11, 12, 13, GLOSSARY | new: p03 `test_perception.py::test_how_a_move_reads_from_where_you_are`; p04 `test_packet.py::test_nothing_later_than_the_moment_reaches_a_mind`, `test_affordances.py` (`test_a_gap_has_nothing_to_close`, `test_the_dead_are_not_people`, `test_skull_10_nobody_is_known_before_they_are_seen`); p07 `test_location_view.py::test_the_dice_receipt_names_a_defence_too`; p10 `test_infected.py` (`test_the_dead_coming_near_are_news`; still verbs in `test_what_the_dead_see`), `test_region.py` (frontages; `test_every_place_name_reads_after_in_or_from`), `test_new_life.py::test_a_generated_world_plays` |
| 2026-09-24 | P10 levels of detail and routes (DECISIONS D-67..D-69) | The dead go back into the count, and a route never doubles back: **P2** `physical/space.py` (a route enters each place at most once, D-69); **P10** `world/hordes.py` (HRD-18 `fold`: an anonymous, unhurt body going nowhere of its own, out of contact, goes back into its crowd's or its district's count — one hunting or drawn by a noise walks on until it arrives; HRD-07: a milling crowd's bodies stand where they were shown, the last of a spent walking crowd walk on; HRD-11 counts the bodies in the world; HRD-15 lists fold), `world/infected.py` (the step folds first; `active` and the day skip a folded body), `physical/space.py` (`remove_body`); **P7** `turn/select.py` (SEL-01: a loud noise holds its neighbourhood for `LOUD_MEMORY_MS`, 10 minutes of world time); **P9** `turn/timers.py` (TIME-10: the off-screen step brings only bodies with a position up to date); **P0 data** `kernel/schema.sql` (`infected_state.folded_at`), `contracts/events.py` (DEMATERIALIZE, HORDE_REJOINED) | The Mega Horde passing a living player only ever grew in bodies (2,880 in its first hour, 6,720 in its second) and a day of it ran for over ten minutes; the dead drawn to a road walked down another road and back first; docs 03, 04, 06, 12, 13, GLOSSARY | new: p02 `test_space.py::test_a_route_never_enters_a_place_twice`; p10 `test_hordes.py` (`test_a_body_takes_the_road_it_means_to`, `test_a_milling_crowd_stands_where_it_is_shown`, `test_the_dead_nobody_is_near_fold_back_into_their_crowd`, `test_a_body_with_anything_of_its_own_stays_a_body`, `test_a_body_with_somewhere_of_its_own_to_be_keeps_walking`, `test_with_its_crowd_gone_it_folds_into_the_district`, `test_the_ones_that_walk_off_unseen_go_back_into_the_count`, `test_a_roar_long_ago_no_longer_holds_the_moment`) |
| 2026-09-24 | Actor v2 B1 (DECISIONS D-70, D-71) | A person's own prompt and their identity card (Actor Spec AC01–AC04): **P4** `mind/identity.py` (new, implemented: `compile_identity`, IDN-01..05), `contracts/mind.py` (`IdentityCard`, `CardSection`, `CardLine`; `SkullPacket.identity` replaces identity_text, voice_capsule, voice_exemplars, would_never_say, the motive / persona / moral / decision lines, active_traits, writers_notes and capability_lines; `AftermathPacket.identity` replaces identity_text and voice_capsule), `mind/packet.py` (the card, never dropped for budget), `prompts/_actor_core.j2`, `_actor_answer.j2` and `actor_*.j2` (the spec's text; the user message in the spec's order and headings), the portrayal-audit, intake, writeback, reflection and say-my-way templates (the card); **P6** `mind/memory.py` (the aftermath carries the whole card); **P0 data** `PacketRules.token_budget` 6000 / 4000 / 3000; **content** core `actors/mara_voss.yaml` (vigilance as checking, not a sense) | The actor prompt called a person's life a story and obeyed author's notes; contradictions, silence and trait effects never reached a decision; docs 01, 05, 08, 09, 12, 13, GLOSSARY, PROGRESS | new: p04 `test_identity.py` (12 tests); amended: p04 `test_packet.py` (`test_identity_time_and_place`; `test_capability_lines` removed — competence is in the card; the protected fields), p06 `test_memory.py` (`test_the_aftermath_and_the_packet_agree`), fixture `prompt_samples/samples.py` (Mara's card) |
| 2026-09-24 | Actor v2 B2 (DECISIONS D-72, D-73) | A menu from what the person knows, and a packet that says who is where (Actor Spec AC06, AC07, AC14, AC16, §5; fidelity C05): **P4** `mind/affordance.py` (AFF-11; the duty gate never rejects — posts and known laws add cost notes, every note said in order; 'owned' from an owner belief; attack_unarmed only on a weapon seen clearly), `mind/packet.py` (whereabouts; the hour only with a timepiece; heard words cut at `max_heard_chars`; refusals of someone here pinned; `omitted`), `contracts/mind.py` (`PacketEntity.whereabouts`, `SkullPacket.omitted`), `contracts/settings.py` (`PacketRules.max_heard_chars` 800), `contracts/content.py` (`LawEffect` says what forbid and cost mean), `mind/firewall.py` (entrenched = the moral gate), `prompts/actor_cognition.user.j2` (the entity line ends with where they are); **P6** `mind/memory.py` (the aftermath's people carry whereabouts; its words are cut as the packet's); **P7** `turn/cognition.py` (entrenched_block reads the moral gate only); **content** core `items/gear.yaml` (wristwatch, tag timepiece), `laws/settlement_core.yaml` and `affordances/items.yaml` (comments) | A stranger never weighed a law he did not know, a member never weighed one at all, and the menu leaked locks-by-record, owners on record and hands out of sight; relatives read as present; everyone knew the clock; docs 05, 06, 09, 12, 13, GLOSSARY, README_FIRST | new: p04 `test_knowledge_menus.py` (a dict scenario; fixture pack `knowing_laws`); amended: p04 `test_packet.py` (`test_identity_time_and_place`; new `test_only_a_timepiece_tells_the_hour`, `test_present_heard_and_last_seen_are_different_things`, `test_a_flood_of_words_is_cut_where_a_word_ends`; `omitted` in the budget test), p06 `test_retrieval.py` (new `test_what_bears_on_the_moment_is_never_cut`; `omitted` order), `test_memory.py` (new `test_a_flood_of_words_is_remembered_as_it_was_heard`), fixture `prompt_samples/samples.py` (whereabouts) |
| 2026-09-24 | Actor v2 B3 (DECISIONS D-74, D-75) | The answer format, one consultation, and the hold (Actor Spec §7, §8, §14, AC05, AC15): **P1** `contracts/mind.py` (`ActorReplyV2` with its V1 adapter, `ActionPayload`, `SpeechOut.delivery` / `timing`, `Inscription`, `Consultation`; `SkullPacket.families` / `consult_kinds` / `looked_up`), `lanes/schemas.py` (the V2 cognition schema; SCHEMA-04), `lanes/repair.py`, `lanes/requests.py`, `testing/fake_lm.py` (V2 answers by default); **P4** `mind/consult.py` (new: CONSULT-01..04, 06), `mind/affordance.py` (24 options; paces; the pool behind the menu), `mind/packet.py` (families, consult_kinds, looked_up, `consulted=`), `action/intent.py` (INTENT-07..09; `plan_continuation(accepted_only=)`), `contracts/content.py` (`AFFORDANCE_FAMILIES`, `AffordanceDef.paces` / `family`), `contracts/settings.py` (`PacketRules.max_affordances` 24); **P6** `mind/consult.recall` (CONSULT-05); **P7** `turn/cognition.py` (REPLY-01..02, HOLD-01..02, `DecisionHeld`, `consequential`; ECHO-02 keeps the original line), `turn/pipeline.py` (a held decision rolls the turn back), `turn/intake.py`, `action/effects.py` (pace's check modifier); prompts `_actor_answer.j2`, `_actor_core.j2`, `actor_cognition.user.j2`; **content** core affordances gain `paces` | A flat answer with free-text manner, unlimited speech and a fallback that chose for the person; docs 04, 05, 08, 12, 13, GLOSSARY | new: p04 `test_consult.py`, `test_intent_v2.py`; p06 `test_recall.py`; p07 `test_decision_v2.py`; amended: p01 `test_schemas.py` (the V2 schema), p04 `test_affordances.py` (24 options), `test_intent.py`, fixture `prompt_samples/samples.py` |
| 2026-09-24 | owner: commit the working engine | The engine is built through P10 (Actor v2 steps 1–3 included) and committed on the owner's instruction; the builder records the gates and builds what is left (13 §4.0). Bodies live in `_impl_*.py` files bound at the end of the module that owns the contract (AGENTS.md §4). `kernel/store.Tx.commit_event` gains a private keyword `_replay` (replay re-commits recorded events); `action/intent.plan_continuation` returns None when nothing is offered; `tools/as/gate.py` leaves `_impl_*.py` out of the docstring snapshot | Every body was a stub | AGENTS.md, 13, PROGRESS, README_FIRST, HANDOFF | none (all P0–P10 contract tests and the soak pass) |
| 2026-09-24 | owner: sessions browser and hard delete (DECISIONS D-76) | **P8** `service/runs.py` (`wipe_tree`, new; `delete_run` wipes; `list_runs` gives `final`; RUN-12, RUN-13), `service/game_service.py` (`on_run_delete`: busy first, closes the open run, `run_deleted`; logs name no run), `contracts/view.py` (`RunSummaryView.final`), `contracts/protocol.py` (`run_deleted`, `OutRunDeleted`), the Play UI (`SessionsScreen.vue` from Home's **Your lives**; 10_UI §2.2, §2.2.1, §4); **P10** `kernel/store.py` (`Store.backup_to(..., as_world=)`), `world/worldgen/pipeline.py` (the genesis names no run) | Load was a list on Home; the open run could not be deleted; delete was an rmtree; a world named the run that made it; docs 10, 12, 13, GLOSSARY, README_FIRST, the smoke checklist | new: p08 `test_sessions.py`, p10 `test_world_names_no_run.py`, vitest `sessions.spec.js`; amended: p08 `test_runs_protocol.py` (`test_delete`), `connect_home.spec.js`, `clarity.spec.js`, fixture `runs.json` (`final`) |
| 2026-09-24 | owner: the easy model picker (DECISIONS D-81) | **P8** Connect / Settings → Models (10_UI §2.1): Main model / Second model, picked from the one LM Studio list, the address under Advanced, a missing model replaced from the list; `contracts/settings.default_lanes` names; `as_config.example.yaml` (both lanes on localhost via LM Link) | Two machines framed as strong and fast, a laptop address to edit, typed model ids; docs 08 §2, 11, GLOSSARY, the smoke checklist | amended: vitest `connect_home.spec.js` (the Connect tests; a new test for a model no longer loaded) |
| 2026-09-24 | owner: appearance and clothing, F1a (DECISIONS D-82) | Specified, not built (13 §4.0 step 1 says where): **P0 data** `contracts/dossier.py` (`Looks`, `VisibleMark`, `OutfitPiece`; `Appearance.looks`), `contracts/content.py` (`ClothingProps`, `ItemDef.clothing`), `contracts/events.py` (BODY_CONDITION), `contracts/mind.py` (`PacketEntity.appearance`), `kernel/schema.sql` (bodies.looks, grime, blood, gore, wet, washed_at — SCHEMA_VERSION unchanged); **P2** `physical/bodies.py` (create's looks and the dead's condition, `looks_of`, `condition_of`, `soil`, `rise` keeps the face; LOOK-01, LOOK-04), `physical/objects.py` (`worn`, `coverage`, `visible_gear`, `dress`; LOOK-02), `testing/scenario.py` (`dress`, `looks`; the scenario's dead), `content/pack.py` (CNT-12 clothing, CNT-17); **P3** `mind/perception.py` (`appearance_text`, LOOK-03), `mind/cues.py` (`APPEARANCE_CUES`, `appearance_cues`, LOOK-05); **P4** `mind/packet.py` (LOOK-06), `prompts/actor_cognition.user.j2` (the appearance line); **P5** `mind/cues.cues_of`; **P6** `mind/memory.py` (the aftermath's people carry it too); **P10** `world/worldgen/opening.py` (the PC dressed), `world/infected.py` (the risen keep their face and clothes); **content** core `items/clothing.yaml` (new, 36 items), `items/gear.yaml` (clothing blocks), the thirteen people (`appearance.looks`; worn clothing moved from `starting_inventory` to the outfit), `cues.yaml` (the appearance cues); cheat_admin `fredrick.yaml` (looks); `as_content/templates/actor_template.yaml` | People were height and build to each other; clothes were loose items with no idea where they were worn; nothing kept blood, gore, grime or rain on anyone; docs 09, 12, 13, GLOSSARY, HANDOFF | new: p02 `test_looks.py`, p03 `test_appearance.py`, p04 `test_appearance_in_packet.py`, p05 `test_appearance_cues.py`, p10 `test_dressed.py` |
| 2026-09-24 | owner: smell and gore camouflage, F1b (DECISIONS D-83) | Specified, not built (13 §4.0 step 1): **P0 data** `contracts/settings.py` (`OlfactionRules`, `RulesConfig.olfaction`; `InfectedRules.gore_mask_min`, `mask_window_s`, `mask_break_db`), `contracts/mind.py` (`PacketEntity.appearance` says what is smelled too); **P3** `sense/olfaction.py` (new: `odour_of`, `smell_range_m`, `smells`, `ODOUR_KINDS`, `ODOUR_WORDS`; SMELL-01..03), `mind/perception.py` (`smell_text`, SMELL-04; the standing view's smell and the olfactory detail, SMELL-05); **P4** `mind/packet.py` (LOOK-06 with the smell); **P5** `mind/cues.py` (`SMELL_CUES`, SMELL-06); **P10** `world/infected.py` (INF-14 gore camouflage in `sees`); **content** core `cues.yaml` (smell cues), `lore/shamblers.md` (the truth and a belief about gore) | Nobody smelled anything; gore meant nothing to the dead; docs 05, 06, 07 §4.1, 12, 13, GLOSSARY, HANDOFF, PROGRESS | new: p03 `test_smell.py`, p10 `test_gore_mask.py`; amended (this kit's own new files): p04 `test_appearance_in_packet.py`, p05 `test_appearance_cues.py` |
| 2026-09-24 | owner: human people, H1 (DECISIONS D-84) | Specified, not built (13 §4.0 step 1): **P0 data** `contracts/dossier.py` (`Temper`; `ActorDossier.temper`), `contracts/mind.py` (`CardSection` key 'temper', `PacketEntity.feeling`, `SkullPacket.outburst`), `contracts/events.py` (TEMPER_CHANGE, QUARREL, LOOP_STRENGTH), `contracts/settings.py` (`TemperRules`, `RulesConfig.temper`; drains 'held_temper'; salience 'grievance_near'; `SocietyRules.quarrel_base`, `quarrel_per_stress`, `brawl_chance`), `contracts/content.py` (`AffordanceRequires.can_run`, `infected_within_m`; cascade kind 'adjust_stress'), `contracts/common.py` (`MoralTag.FEED_TO_DEAD`), `kernel/schema.sql` (tempers — SCHEMA_VERSION unchanged), `kernel/ownership.py`; **P2** `physical/bodies.py` (capacity `can_run`); **P4** `mind/identity.py` (the 'temper' section, FUSE_WORDS / OUTLET_WORDS / GRUDGE_WORDS), `mind/affordance.py` (the two gates; AFF-07 rank 3 for 'feed_to_dead'), `prompts/_actor_core.j2` (a person, not a planner), `prompts/actor_cognition.user.j2` (the feeling and the snap); **P5** `mind/temper.py` (new, TEMPER-01..08), `mind/mind.py` (`strengthen_loop`, LOOP-07), `mind/packet.py` (TEMPER-08), `action/effects.py` (`shove_toward`, the leg shot; EFFECT_IDS, SEEN), `action/cascade.py` (adjust_stress dispatch); **P7** `turn/select.py` (SEL-02, SEL-03), `turn/cognition.py` (decide step 4), `turn/pipeline.py` (S3b); **P9** `society/settlement.py` (`friction`, STL-15; STL-03 step 8b), `world/rumours.py` (claims fed_someone_to_the_dead, lost_it, fell_out); **content** core `cascade/stress.yaml` (new, CAS-019..023), `affordances/combat.yaml` (shove_toward_dead, shoot_leg), `affordances/movement.yaml` (running needs can_run), the fourteen people (`temper`; some relationships sharpened; `feed_to_dead` in the lines of those who never would; Nita swings), cheat_admin `fredrick.yaml` | People acted like planners: nobody lost their temper, held a grudge, picked a fight or sold anyone out; stress was never read; docs 05 §7.1, 09 §3.2, 12 §3.6, 13, GLOSSARY, HANDOFF, PROGRESS | new: p05 `test_temper.py`, `test_temper_in_packet.py`, `test_betrayal.py`, p07 `test_breaking_point.py`, p09 `test_quarrels.py`; amended: p04 `test_identity.py` (the temper section) |
